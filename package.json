{
  "name": "curly",
  "version": "0.1.0",
  "description": "A document database modeller that runs in the browser",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "check": "tsc --noEmit",
    "test": "node --test tests/*.test.ts",
    "serve": "python3 -m http.server 8000"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
